import HeroSection from "./components/HeroSection";

type Props = {};

const Home = (props: Props) => {
  return (
    <div className="">
      <HeroSection />
    </div>
  );
};

export default Home;
